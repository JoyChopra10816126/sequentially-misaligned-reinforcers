from setuptools import setup, find_packages

setup(
    name="sequentially_misaligned_reinforcers",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],  # list dependencies here
    python_requires='>=3.8',
    author="Joy K. Chopra",
)